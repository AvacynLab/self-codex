/**
 * @file Fixture leveraged by the lint configuration tests to provide a
 * concrete TypeScript path that belongs to the ESLint project graph. The file
 * content is intentionally minimal; the tests override it with custom code via
 * `lintText` to assert rule behaviour without mutating repository sources.
 */
export const lintFixtureValue = 0;
